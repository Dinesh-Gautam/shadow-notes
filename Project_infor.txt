; * = done ; () = pending ; ^ = maybe in futer ; # = cancle;

Use React routers   ()
improve auth code   *

//////

pagination

firebase :
    add security rules (add timeLimit for uploading a new note)